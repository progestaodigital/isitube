# Database location used by Prisma CLI during development.
# At runtime, the main process overrides this to point to the user's
# AppData directory (production) or prisma/dev.db (dev).
DATABASE_URL="file:./dev.db"
